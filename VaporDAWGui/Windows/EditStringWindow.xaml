<Window x:Class="VaporDAWGui.EditStringWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VaporDAWGui"
        mc:Ignorable="d"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        Title="Edit String" Width="300">
    <!--Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"-->
    <StackPanel Margin="10" Orientation="Vertical">
        <Label Content="-" Name="stringLabel" HorizontalAlignment="Left" />
        <TextBox  Name="stringTextBox"  />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Width="80" Name="okButton" IsDefault="True">Ok</Button>
            <Button Width="80" Name="cancelButton" IsCancel="True" Margin="10,0,0,0">Cancel</Button>
        </StackPanel>
    </StackPanel>
</Window>
